import { GetPropertyListPipe } from './get-property-list.pipe';

describe('GetPropertyListPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPropertyListPipe();
    expect(pipe).toBeTruthy();
  });
});
