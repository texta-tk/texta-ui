import { GroupSameValuesPipe } from './group-same-values.pipe';

describe('GroupSameValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupSameValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
