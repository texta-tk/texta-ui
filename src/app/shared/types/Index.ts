export interface Index {
  id: number;
  is_open: boolean;
  name: string;
  doc_count: number;
  size: number;
  url: string;
  added_by: string;
  client: string;
  description: string;
  created_at: string;
  source: string;
  domain: string;
  test: boolean;
  has_validated_facts: boolean;
}
