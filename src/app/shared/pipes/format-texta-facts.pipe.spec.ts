import { FormatTextaFactsPipe } from './format-texta-facts.pipe';

describe('FormatTextaFactsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextaFactsPipe();
    expect(pipe).toBeTruthy();
  });
});
